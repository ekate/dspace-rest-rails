<div class="jumbotron">

    <div class="row">
        <div class="col-md-6">
            <ul>
                <% @items.each do |item|%>
                    <li><%= link_to "#{item['name']} - #{item['handle']}", item_path(:id => item['id'])  %></li>
                <% end %>
            </ul>
        </div>
    </div>


</div>